name: hello_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0-139.0.dev

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ✅ 新增 Google Maps Flutter 插件
  google_maps_flutter: ^2.7.0  

  flutter_polyline_points: ^2.0.0
  
  file_picker: ^8.0.0  

  geolocator: ^12.0.0

  google_fonts: ^6.2.1 # 或者最新的版本

  audioplayers: ^6.1.0

  image_picker: ^1.0.7


dev_dependencies:
  flutter_test:
    sdk: flutter



  flutter_lints: ^6.0.0
  
flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/music/alarm.mp3